<for|row| of=input.rows>
  <div
    class=(input.selectedId === row.id ? "selected" : undefined)
    data-key=row.id
    data-selected=(input.selectedId === row.id ? "true" : undefined)
    key=row.id>
    ${row.label}
  </div>
</for>
