export const title = "Forms and Validation";
export const description = "Reactive form state, server errors, schema validation, and mutation choices.";

# Forms and validation

Use `@reckona/mreact-forms` for reactive form state and field-level errors. Use route handlers for explicit JSON or multipart APIs, and server actions for form-first mutations that should be bound to rendered action references.

Keep validation close to the mutation boundary.
