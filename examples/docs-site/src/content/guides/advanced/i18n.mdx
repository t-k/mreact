export const title = "i18n";
export const description = "Locale detection and typed message bundles.";

# i18n

Use `defineMessages()` for typed message bundles and `detectLocale()` to derive locale from route prefixes or request headers. Keep UI strings centralized rather than hard-coding translated text in components.
