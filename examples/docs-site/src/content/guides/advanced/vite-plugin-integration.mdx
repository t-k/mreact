export const title = "Vite Plugin Integration";
export const description = "How user Vite plugins participate in server, client, Cloudflare, and prerender builds.";

# Vite plugin integration

The router forwards user Vite plugins into app-router bundle builds after removing the router plugin itself. This allows content transforms, MDX, CSS tooling, and custom loaders to participate in server, client, Cloudflare, and prerender builds.
