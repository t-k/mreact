export const title = "Metadata and Head";
export const description = "Set titles, descriptions, Open Graph, icons, robots, viewport, and custom head tags.";

# Metadata and head

Routes can export static `metadata` or `generateMetadata(context)`. Metadata composes through layouts, can read loader data, and supports title, description, canonical links, Open Graph, icons, robots, theme color, viewport, CSP, security headers, and custom head descriptors.
