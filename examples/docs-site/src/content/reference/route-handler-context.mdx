export const title = "Route Handler Context";
export const description = "Params, request, route, env, and adapter-specific context.";

# Route handler context

Route handlers receive decoded params as their second argument. The context also includes the current request, matched route, and adapter environment where available. Cloudflare adds Worker bindings and execution context.
