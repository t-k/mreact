export const title = "Config";
export const description = "mreactRouter Vite plugin options.";

# Config

Core project options include `projectRoot`, `routesDir`, `publicDir`, `allowedSourceDirs`, `buildTargets`, `clientSourceMaps`, `assetBaseUrl`, `publicAssetBaseUrl`, and `production.dropClientConsole`.
