export const title = "Route Module Exports";
export const description = "Supported exports from page, layout, template, middleware, and route modules.";

# Route module exports

Common exports include `loader`, `metadata`, `generateMetadata`, `prerender`, `generateStaticParams`, `revalidate`, `stream`, `slots`, middleware `config`, and route handler method exports.
