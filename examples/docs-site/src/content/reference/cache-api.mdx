export const title = "Cache API";
export const description = "Route cache and revalidation helper reference.";

# Cache API

Use `revalidate`, `cacheControl()`, and `revalidatePath()` to control route HTML caching and invalidation.
