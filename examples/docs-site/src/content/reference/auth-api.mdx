export const title = "Auth API";
export const description = "Session and authorization helper reference.";

# Auth API

`@reckona/mreact-auth` exports session helpers, `configureAuth`, `getCurrentSession`, `requireSession`, `requireRole`, `requirePermission`, `tryRequireRole`, `tryRequirePermission`, and browser-safe session claims helpers.
