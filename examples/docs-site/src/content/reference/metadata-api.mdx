export const title = "Metadata API";
export const description = "RouteMetadata fields and head descriptor support.";

# Metadata API

`RouteMetadata` supports title, description, canonical links, Open Graph, icons, robots, theme color, viewport, CSP, security headers, language, and arbitrary head descriptors for base, link, meta, script, and style tags.
