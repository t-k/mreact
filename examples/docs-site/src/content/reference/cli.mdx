export const title = "CLI";
export const description = "mreact-router command overview.";

# CLI

Use `mreact-router --help`, `mreact-router help build`, or command-level help to inspect supported flags. Common commands are `dev`, `build`, `start`, and `package`.
