export const title = "Response Helpers";
export const description = "Small helpers for redirects, errors, forms, cache, and CSRF.";

# Response helpers

Router helpers include `redirect`, `redirect303`, `textError`, `parseForm`, `cacheControl`, `revalidatePath`, and CSRF helpers for form and multipart workflows.
