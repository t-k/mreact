{
  "extends": "../tsconfig.base.json",
  "compilerOptions": {
    // This compatibility example intentionally checks source code against React's public types;
    // package.test.ts separately enforces that Vite redirects the runtime to mreact.
    "jsxImportSource": "react"
  },
  "include": ["src", "vite.config.ts"]
}
