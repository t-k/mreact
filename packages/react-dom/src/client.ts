/** Creates a concurrent DOM root for rendering into a container. */
export { createRoot } from "@reckona/mreact-compat";
/** Hydrates server-rendered markup in a DOM container. */
export { hydrateRoot } from "@reckona/mreact-compat";
/** Options used when hydrating server-rendered markup. */
export type { HydrateRootOptions } from "@reckona/mreact-compat";
/** Render root returned by createRoot and hydrateRoot. */
export type { Root } from "@reckona/mreact-compat";
/** Options used when creating a DOM render root. */
export type { RootOptions } from "@reckona/mreact-compat";

/** React DOM client package version. */
export const version = "19.2.6";
