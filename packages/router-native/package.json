{
  "name": "@reckona/mreact-router-native",
  "version": "0.0.208",
  "description": "Native addon loader for mreact router runtime hot paths.",
  "keywords": [
    "addon",
    "jsx",
    "mreact",
    "native",
    "router",
    "typescript"
  ],
  "homepage": "https://github.com/t-k/mreact/tree/main/packages/router-native#readme",
  "bugs": {
    "url": "https://github.com/t-k/mreact/issues"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/t-k/mreact.git",
    "directory": "packages/router-native"
  },
  "files": [
    "index.cjs",
    "index.d.ts"
  ],
  "type": "commonjs",
  "sideEffects": false,
  "main": "./index.cjs",
  "types": "./index.d.ts",
  "exports": {
    ".": {
      "types": "./index.d.ts",
      "require": "./index.cjs",
      "default": "./index.cjs"
    }
  },
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "cargo build --release && node scripts/copy-binary.cjs",
    "test": "cargo test --locked --no-default-features",
    "test:clippy": "cargo clippy --locked --all-targets --all-features -- -D warnings",
    "test:deny": "cargo deny check bans licenses sources",
    "test:deny:advisories": "cargo deny check advisories",
    "test:deny:all": "pnpm test:deny && pnpm test:deny:advisories && pnpm test:deny:fuzz && pnpm test:deny:fuzz:advisories",
    "test:deny:fuzz": "cargo deny --manifest-path fuzz/Cargo.toml check bans licenses sources",
    "test:deny:fuzz:advisories": "cargo deny --manifest-path fuzz/Cargo.toml check advisories",
    "test:doc": "cargo test --locked --doc --no-default-features",
    "test:fuzz:base64": "node scripts/run-fuzz.mjs campaign decode_flight_base64 60",
    "test:fuzz:build": "node scripts/run-fuzz.mjs build",
    "test:fuzz:rows": "node scripts/run-fuzz.mjs campaign decode_flight_rows 60",
    "test:fuzz:smoke": "node scripts/run-fuzz.mjs smoke",
    "test:fuzz:campaign": "node scripts/run-fuzz.mjs campaign",
    "test:mutants": "pnpm test:mutants:all",
    "test:mutants:base64": "cargo mutants --config .cargo/mutants-base64.toml --test-tool nextest --no-default-features --jobs 2 --timeout 60",
    "test:mutants:routes": "cargo mutants --config .cargo/mutants-routes.toml --test-tool nextest --no-default-features --jobs 2 --timeout 60",
    "test:mutants:flight": "cargo mutants --config .cargo/mutants-flight.toml --test-tool nextest --no-default-features --jobs 2 --timeout 60",
    "test:mutants:all": "cargo mutants --config .cargo/mutants.toml --test-tool nextest --no-default-features --jobs 2 --timeout 60",
    "test:nextest": "cargo nextest run --locked --no-default-features",
    "test:quality:quick": "pnpm test:clippy && pnpm test:nextest && pnpm test:doc && pnpm test:deny:all"
  },
  "optionalDependencies": {
    "@reckona/mreact-router-native-darwin-arm64": "workspace:*",
    "@reckona/mreact-router-native-linux-x64-gnu": "workspace:*",
    "@reckona/mreact-router-native-win32-x64-msvc": "workspace:*"
  }
}
