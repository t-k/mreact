{
  "name": "server stream out of order await",
  "target": "server",
  "serverOutput": "stream",
  "code": "export function App() { const name = Promise.resolve(\"Ada\"); return <section>Before<Await value={name} placeholder={<span>Loading</span>}>{value => <span>{value}</span>}</Await><p>After</p></section>; }",
  "expected": {
    "diagnostics": [],
    "imports": [
      {
        "source": "@reckona/mreact-server",
        "specifiers": ["renderOutOfOrderBoundary"]
      }
    ],
    "serverStreamHtml": "<section>Before<template data-mreact-oob-placeholder=\"mreact-0\"><span>Loading</span></template><p>After</p></section><template data-mreact-oob-fragment=\"mreact-0\"><span>Ada</span></template>"
  }
}
